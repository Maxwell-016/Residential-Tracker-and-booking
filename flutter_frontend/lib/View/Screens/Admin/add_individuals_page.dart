import 'package:flutter/material.dart';

class AddIndividualsPage extends StatelessWidget {
  const AddIndividualsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add Individuals')),
      body: const Center(child: Text('Add Individuals Page')),
    );
  }
}