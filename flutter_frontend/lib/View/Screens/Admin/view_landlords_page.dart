import 'package:flutter/material.dart';

class ViewLandlordsPage extends StatelessWidget {
  const ViewLandlordsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('View Landlords')),
      body: const Center(child: Text('View Landlords Page')),
    );
  }
}